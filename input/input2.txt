main
num V_a, num V_b, num V_c,
begin
    V_a = 10;
    V_b = 20;
    if eq(V_a, V_b) then
        begin
            V_c = F_add(V_a, V_b, 5);
        end
    else
        begin
            V_c = F_subtract(V_b, V_a, 6);
        end;
end

num F_add(V_x, V_y, V_z) {
    num V_1, num V_2, num V_result,
    begin
        V_1 = add(V_x,V_y);
        V_2 = add(V_1, V_z);
        return V_2;
    end
}

num F_subtract( V_a, V_b, V_c) {
    num V_diff, num V_x, num V_y,
    begin
        V_diff = sub(V_a,V_b);
        return V_diff;
    end
}

num F_sqrt( V_str1, V_str2, V_num) {
    num V_result, num V_x, num V_y,
    begin
        skip;
        return V_result;
    end
}
end
